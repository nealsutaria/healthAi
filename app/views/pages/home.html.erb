<div class="min-h-screen bg-white flex flex-col justify-start px-4 sm:px-6 lg:px-8">

  <!-- Hero Section -->
  <main class="py-12 sm:py-16 lg:py-20 text-center">
    <div class="max-w-2xl mx-auto space-y-4">
      <h1 class="text-3xl sm:text-5xl font-extrabold text-gray-900 leading-tight tracking-tight">
        Your Health Records, Smarter.
      </h1>
      <p class="text-base sm:text-lg text-gray-600">
        MHBright is your AI-powered health dashboard — track visits, upload results, and get personalized insights. Private. Secure. Instant.
      </p>

      <!-- CTA Buttons -->
      <div class="mt-6 flex flex-col sm:flex-row justify-center items-center gap-4">
        <%= link_to "Create Your Free Account", new_user_registration_path,
              class: "w-full sm:w-auto bg-indigo-600 hover:bg-indigo-500 text-white font-semibold px-6 py-3 rounded-xl text-sm sm:text-base transition shadow" %>

        <%= link_to "Log In", new_user_session_path,
              class: "text-indigo-600 hover:underline font-medium text-sm sm:text-base" %>
      </div>
    </div>
  </main>

  <!-- Features Section (Raised on Desktop Only) -->
  <section class="w-full max-w-5xl mx-auto mt-8 sm:mt-10 lg:-mt-8 pb-12 transition-all duration-300">
    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-5 gap-4 sm:gap-6 px-2 sm:px-0">
      <% features = [
        ["🧠", "AI Doctor Suggestions", "Get alerts from Gemini based on your symptoms & visits."],
        ["📸", "Image Scan Insights", "Upload test results or prescriptions — Gemini explains them."],
        ["💊", "Smart Record Storage", "Keep all your health data organized, accessible & safe."],
        ["🔒", "Secure & Private", "End-to-end encrypted. You own your data. No ads."],
        ["💬", "24/7 AI Health Chat", "Ask health questions anytime, powered by AI."]
      ] %>

      <% features.each do |icon, title, desc| %>
        <div class="bg-gray-50 rounded-2xl p-4 sm:p-6 text-center border hover:shadow-md transition">
          <div class="text-3xl mb-2"><%= icon %></div>
          <h3 class="text-sm sm:text-base font-semibold text-gray-900 mb-1"><%= title %></h3>
          <p class="text-xs sm:text-sm text-gray-600 leading-snug"><%= desc %></p>
        </div>
      <% end %>
    </div>
  </section>
</div>





